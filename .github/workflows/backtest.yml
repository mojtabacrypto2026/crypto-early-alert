name: Nobitex Backtest

on:
  workflow_dispatch:

  push:
    branches:
      - main
    paths:
      - "backtest.py"
      - ".github/workflows/backtest.yml"

jobs:
  backtest:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install requests

      - name: Fix Nobitex DNS
        run: |
          echo "185.143.232.201 api.nobitex.ir" | sudo tee -a /etc/hosts
          echo "185.143.234.120 api.nobitex.ir" | sudo tee -a /etc/hosts
          getent hosts api.nobitex.ir

      - name: Fix Nobitex symbol format
        run: |
          sed -i 's/"symbol": market,/"symbol": market.replace("-", "").upper(),/' backtest.py

      - name: Run backtest
        run: python backtest.py

      - name: Save backtest results
        uses: actions/upload-artifact@v4
        with:
          name: nobitex-backtest-results
          path: backtest_results.csv
